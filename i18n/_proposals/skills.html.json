{
  "file": "skills.html",
  "_note": "policy=translate (i18n/policy.json). skills.html has exactly one <pre>, in #template, and it is frozen by gate B, so the Chinese SOP template is proposed here instead of edited in en/. Field labels follow the binding terms table /home/user/out/terms_nginx_proxy_manager.txt (Code placeholders section, row 'skills.html#template SKILL 範本'): 名稱=name, 目的=purpose, 輸入=inputs, 前置檢查=preflight checks, 步驟=steps, 輸出=outputs, 停止條件=stop conditions; npm-certificate-auditor is kept verbatim. Line count, list markers (-) and step numbering (1.-4.) are identical to the zh block, and the full-width colon becomes an ASCII colon plus one space per i18n/STYLE.en.md. The zh block occurs exactly once in both skills.html and en/skills.html, so scripts/apply_code_translations.js passes its uniqueness guard; no escalation needed. ACCEPTANCE GATE: check_i18n.js stripForCjk() removes <pre>/<code>/<kbd> before the gate C scan, so this Chinese ships silently behind a green check if the proposal is never applied. Do not --accept en:skills.html#template until `grep -c '名稱：npm-certificate-auditor' en/skills.html` returns 0. NOTE on the policy brief ('any prompt demanding Traditional Chinese output becomes English-only'): this template contains no output-language instruction at all, so there is nothing to rewrite for that clause — no second entry is filed.",
  "entries": [
    {
      "unit": "#template",
      "zh": "<pre>名稱：npm-certificate-auditor\n目的：唯讀盤點 NPM 憑證，依到期日與引用關係產生報告\n\n輸入：\n- NPM 實例識別與環境標籤\n- 經驗證、版本相符的唯讀資料取得方式\n- 告警門檻：7、14、30 天\n\n前置檢查：\n1. 驗證目標實例、版本、部署方式與唯讀權限\n2. 驗證介面 schema；不能驗證就停止，不猜 endpoint\n3. 確認輸出位置不會洩漏祕密\n\n步驟：\n1. 取得憑證中繼資料與主機引用關係\n2. 計算到期天數，核對網域名稱與最近續期錯誤\n3. 依門檻分級，產生含資料時間與證據的報告\n4. 遮罩 Token、Cookie、私鑰與不必要個資\n\n輸出：\n- 摘要、風險項、證據、建議人工處置\n- 不直接申請、續期、刪除或重新指派憑證\n\n停止條件：\n- 版本或 schema 不符\n- 回應缺少必要欄位\n- 目標不是核准的實例\n- 取得資料時要求提升權限</pre>",
      "en": "<pre>name: npm-certificate-auditor\npurpose: take a read-only inventory of NPM certificates and report on them by expiry date and reference\n\ninputs:\n- NPM instance identifier and environment label\n- a verified, version-matched, read-only way to get the data\n- alert thresholds: 7, 14, 30 days\n\npreflight checks:\n1. Verify the target instance, the version, the deployment option and the read-only permissions\n2. Verify the interface schema; stop if you cannot verify it, and never guess an endpoint\n3. Confirm the output location cannot leak secrets\n\nsteps:\n1. Fetch the certificate metadata and the hosts that reference each certificate\n2. Compute the days to expiry, and check the domain names and any recent renewal errors\n3. Grade against the thresholds and produce a report carrying the data time and the evidence\n4. Redact tokens, cookies, private keys and any unnecessary personal data\n\noutputs:\n- a summary, the risk items, the evidence and the suggested manual handling\n- never request, renew, delete or reassign a certificate directly\n\nstop conditions:\n- the version or the schema does not match\n- the response is missing a required field\n- the target is not an approved instance\n- fetching the data would require elevated permissions</pre>"
    }
  ]
}

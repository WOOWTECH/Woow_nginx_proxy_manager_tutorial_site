{
  "file": "ch9_access_lists.html",
  "_note": "Chinese placeholder inside a frozen code block (gate B). English form is verbatim from the binding terms table /home/user/out/terms_nginx_proxy_manager.txt (Code placeholders section): <可信任 CIDR> -> <trusted-cidr>. BLOCKER FOR THE APPLIER/ORCHESTRATOR OWNER — this entry cannot be applied by the current tool. The block occurs TWICE in #ip-rules (rows 1 and 3 of the rule-order table) and both occurrences are byte-identical, so scripts/apply_code_translations.js rejects it at its uniqueness guard (`const n = html.split(e.zh).length - 1; if (n !== 1) { reject }`, lines 59-64) and writes nothing. Re-running the applier does NOT help: the guard fires before html.replace(), so run 1 changes nothing and run 2 sees the same 2 occurrences and fails identically. Widening the zh string to disambiguate the two rows is also invalid: check_i18n.js:203 matches the gate B whitelist on `w.zh === c`, where c is the extracted <code>…</code> block itself, so any zh spanning outside the code block would apply and then fail gate B. REQUIRED FIX (outside en/ and outside this proposal, hence the escalation): apply_code_translations.js must replace globally — change the guard to `if (n < 1)` and the replace to `html = html.split(e.zh).join(e.en);`. This entry stays at exactly one row: gate B (check_i18n) matches per code block on (file, unit, zh, en), so one whitelist row covers both blocks once both are replaced. ACCEPTANCE GATE: gate C never sees code blocks (check_i18n.js stripForCjk strips <pre>/<code>/<kbd>), so unapplied Chinese here ships silently with a green check. Do not accept en/ch9_access_lists.html until `grep -c '可信任 CIDR' en/ch9_access_lists.html` returns 0.",
  "entries": [
    {
      "unit": "#ip-rules",
      "zh": "<code>allow &lt;可信任 CIDR&gt;</code>",
      "en": "<code>allow &lt;trusted-cidr&gt;</code>"
    }
  ]
}
